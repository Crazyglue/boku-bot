apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "boku-ds-devops.fullname" . }}-test-connection"
  labels:
    {{- include "boku-ds-devops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "boku-ds-devops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
