apiVersion: v1
kind: Service
metadata:
  name: {{ include "boku-ds-devops.fullname" . }}
  labels:
    {{- include "boku-ds-devops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "boku-ds-devops.selectorLabels" . | nindent 4 }}
